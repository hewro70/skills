<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Locale
    |--------------------------------------------------------------------------
    | اللغة الافتراضية التي تُستعمل للقراءة/الكتابة. عند تركها null سيتم استخدام
    | app()->getLocale() (أي لغة التطبيق الحالية).
    */
    'locale' => null,

    /*
    |--------------------------------------------------------------------------
    | Use Fallback Locale
    |--------------------------------------------------------------------------
    | في حال عدم وجود ترجمة للغة الحالية، هل نستخدم لغة احتياطية؟
    */
    'use_fallback_locale' => true,

    /*
    |--------------------------------------------------------------------------
    | Fallback Locale
    |--------------------------------------------------------------------------
    | اللغة الاحتياطية التي نرجع لها عند غياب الترجمة. عندما تكون null سيتم استخدام
    | config('app.fallback_locale') من ملف config/app.php.
    */
    'fallback_locale' => null,

    /*
    |--------------------------------------------------------------------------
    | Fallback If Empty
    |--------------------------------------------------------------------------
    | إن كانت الترجمة موجودة لكنها فارغة ("")، هل نرجع إلى اللغة الاحتياطية؟
    */
    'use_fallback_if_empty' => true,

    /*
    |--------------------------------------------------------------------------
    | Default Locale On Set
    |--------------------------------------------------------------------------
    | عند استدعاء setTranslation بدون تمرير لغة، أي لغة تُستخدم؟ عند تركها null
    | سيتم استخدام app()->getLocale().
    */
    'default_locale' => null,

    /*
    |--------------------------------------------------------------------------
    | Strict Mode
    |--------------------------------------------------------------------------
    | في حال محاولة تعيين/قراءة خاصية غير مُعلنة في $translatable بالموديل،
    | هل نرمي استثناء أم نتجاهل؟ اجعلها true أثناء التطوير لو أحببت الصرامة.
    */
    'throw_exception_when_property_is_not_translatable' => false,
];
